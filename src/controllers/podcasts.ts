import { Request, Response } from 'express'

const podcastGet = (req: Request, res: Response) => {
  const {} = req
  return res.json({})
}

const podcastPost = (req: Request, res: Response) => {
  const {} = req
  return res.json({})
}

const podcastPut = (req: Request, res: Response) => {
  const {} = req
  return res.json({})
}

const podcastDelete = (req: Request, res: Response) => {
  const {} = req
  return res.json({})
}

export { podcastGet, podcastPost, podcastPut, podcastDelete }
