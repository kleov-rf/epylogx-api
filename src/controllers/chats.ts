import { Request, Response } from 'express'

const chatsGet = (req: Request, res: Response) => {
  const {} = req
  return res.json({})
}

const chatsPost = (req: Request, res: Response) => {
  const {} = req
  return res.json({})
}

const chatsPut = (req: Request, res: Response) => {
  const {} = req
  return res.json({})
}

const chatsDelete = (req: Request, res: Response) => {
  const {} = req
  return res.json({})
}

export { chatsGet, chatsPost, chatsPut, chatsDelete }
